{
  "id": "004-missing-null-guard",
  "prompt": "The test suite is failing. Find the bug and fix it.",
  "difficulty": "easy",
  "tags": ["null-guard", "undefined-access"],
  "brokenFile": "src/name.ts",
  "groundTruthPatch": "src/name.ts: `user.profile!.name` (wrong, asserts profile exists and throws when it doesn't) instead of `user.profile?.name ?? \"anonymous\"` (right)"
}
