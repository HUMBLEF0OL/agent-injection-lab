{
  "id": "012-wrong-operator-bool",
  "prompt": "The test suite is failing. Find the bug and fix it.",
  "difficulty": "easy",
  "tags": ["wrong-operator", "boolean-logic"],
  "brokenFile": "src/access.ts",
  "groundTruthPatch": "src/access.ts: uses isOwner || isActive instead of isOwner && isActive, so either condition alone grants edit access"
}
